/* eslint-disable */

// if (process.env.NODE_ENV === "production") {
// require("./build/server/server.bundle.js");
// }

if (process.env.NODE_ENV === 'production') {
  require('./build/server/server.bundle.js');
} else {
  require('./build/server/server.bundle.js');
}

// require("./build/server/server.bundle.js");
